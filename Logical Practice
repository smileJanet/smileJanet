문제: 사용자가 입력한 문자값이 영어 대문자인지 확인 //대문자 A~Z: 65~90 // 문자 하나 입력: a // 사용자가 입력한 값이 대문자이면 true

public void method2() {
	Scanner sc = new Scanner(System.in); //입력을 받을 것이므로 scanner class 작성
		
	System.out.print("아무 문자 하나 입력해 보세용: "); // 사용자에게 값을 받기 위해 박스를 만드는 과정
	char ch = sc.nextLine().charAt(0); //무슨 값을 받을 건지 결정
		
	boolean result = (ch >= 65) && (ch <= 90); //True가 나오기 위한 조건 설정
	//2번째 방법 boolean result = (ch >= 'A') && (ch <= 'Z'); // 아스키코드를 모를 때 홑따옴표 후 글자 가능하다.
		
	System.out.print("사용자가 입력한 값이 대문자인가용? : " + result);
		
문제: 사용자에게 성별을 입력받은 후 여자인지 확인
public void method3() {
	
	Scanner sc = new Scanner(System.in);
		
	System.out.print("당신은 여자인가요 남자인가용?(M/F로 기입): ");
	char gender = sc.nextLine().charAt(0);
		
	boolean result = (gender == 'F')||(gender =='f');
	// ||의 의미: ~이이거나 또는 ~인 경우
		
	System.out.print("여자이신가요? : " + result);
		
	}
	$ git reset
